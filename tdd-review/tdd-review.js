
export function takeArrayAndReturnLength(array) {
    return array.length;
}

export function getThirdItem(array) {
    return array.length > 2 ? array[2] : 'empty';
}

export function firstKeyLastValue(obj) {
    const keys = Object.keys(obj);

    return keys;
}
